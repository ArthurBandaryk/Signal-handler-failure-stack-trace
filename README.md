# Signal-handler-failure-stack-trace
Examine stack trace on `glog` check failure for macOS, Ubuntu and Windows using [`failure signal handler`](https://github.com/google/glog#failure-signal-handler).
